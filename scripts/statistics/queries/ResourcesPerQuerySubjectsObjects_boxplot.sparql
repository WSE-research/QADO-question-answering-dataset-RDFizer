PREFIX qab: <urn:qa:benchmark#>

SELECT ?benchmark (GROUP_CONCAT(?resources; SEPARATOR=",") as ?concat) {
    ?question qab:isElementOf ?benchmark;
    qab:numberOfResourcesSubjectsObjects ?resources .
} GROUP BY ?benchmark ORDER BY ?benchmark